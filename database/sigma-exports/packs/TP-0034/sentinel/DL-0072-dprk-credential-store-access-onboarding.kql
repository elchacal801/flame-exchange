// FLAME logsource mapping -- table names and field names require environment-specific tuning.

((event_type contains "credential_store_access" or event_type contains "password_manager_export" or event_type contains "session_token_read" or event_type contains "api_key_create") and user_tenure_days <= 14) and credential_access_count_24h >= 10
