// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "WIRE_INITIATION" and wire_type =~ "international" and beneficiary_country_in_business_profile =~ false and transaction_amount >= 5000) and (not((account_type =~ "personal" and transaction_amount < 3000)))
