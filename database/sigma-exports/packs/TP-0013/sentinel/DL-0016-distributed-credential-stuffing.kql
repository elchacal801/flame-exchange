// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "login_attempt" and auth_result =~ "failure") and user_agent matches regex "^Mozilla/5\\.0.*$"
