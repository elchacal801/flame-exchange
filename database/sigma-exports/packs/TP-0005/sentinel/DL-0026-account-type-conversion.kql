// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "ACCOUNT_TYPE_CONVERSION" and (conversion_type in~ ("savings_to_checking", "personal_to_business", "basic_to_premium"))) and days_since_last_credential_change <= 7
