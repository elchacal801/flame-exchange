// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "ALERT_SETTINGS_CHANGE" and (action in~ ("threshold_increase", "notification_disable", "alert_suppress"))) and (channel in~ ("online_banking", "mobile_app", "api"))
