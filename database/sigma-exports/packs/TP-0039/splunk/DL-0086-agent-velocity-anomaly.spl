` FLAME logsource mapping -- field names, index, and sourcetype require environment-specific tuning. `

initiated_by="ai_agent" (count>=5 groupby="agent_session_id" timeframe="1m") OR ("dc(merchant_id)">=3 groupby="agent_session_id" timeframe="1m")
