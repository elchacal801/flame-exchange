// FLAME logsource mapping -- table names and field names require environment-specific tuning.

source_wallet startswith "" and unique_destination_count >= 100 and timeframe_hours <= 48 and total_amount_usd >= 1000000
