// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "domain_registration" and registration_source =~ "zone_file_diff") and (count >= 15 and groupby =~ "registrant_cluster_id" and timeframe =~ "1h") and inter_registration_stddev <= 2
