/* FLAME logsource mapping -- field names and index patterns require environment-specific tuning. */

domain:xn\-\-* AND (resolved_visual_domain:(*bitpay* OR *coinbase* OR *metamask* OR *phantom* OR *pancakeswap* OR *uniswap* OR *opensea* OR *kraken* OR *binance* OR *ledger*))
