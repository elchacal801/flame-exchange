// FLAME logsource mapping -- table names and field names require environment-specific tuning.

domain startswith "xn--" and (resolved_visual_domain contains "bitpay" or resolved_visual_domain contains "coinbase" or resolved_visual_domain contains "metamask" or resolved_visual_domain contains "phantom" or resolved_visual_domain contains "pancakeswap" or resolved_visual_domain contains "uniswap" or resolved_visual_domain contains "opensea" or resolved_visual_domain contains "kraken" or resolved_visual_domain contains "binance" or resolved_visual_domain contains "ledger")
