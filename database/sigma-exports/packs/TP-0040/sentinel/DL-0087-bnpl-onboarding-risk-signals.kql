// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(account_type =~ "bnpl" and application_status =~ "approved") and email_domain_age_days <= 30 and digital_footprint_score <= 0.2
