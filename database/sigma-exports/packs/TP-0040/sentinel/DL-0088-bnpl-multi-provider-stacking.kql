// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(application_type =~ "bnpl" and application_status =~ "approved") and (count >= 3 and groupby =~ "identity_hash" and timeframe =~ "72h") and dc_provider_id >= 3
