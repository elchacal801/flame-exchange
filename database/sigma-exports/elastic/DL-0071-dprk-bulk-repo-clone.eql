/* FLAME logsource mapping -- field names and index patterns require environment-specific tuning. */

((action:(*repo.clone* OR *repo.download* OR *repo.fork*)) AND actor_account_age_days:<=14) AND (repos_cloned_24h:>=5 OR repo_team_match:false)
