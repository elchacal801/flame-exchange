// FLAME logsource mapping -- table names and field names require environment-specific tuning.

condition in~ ("selection | count() by customer_account_id > 4", "selection | count() by store_region_code > 15")
