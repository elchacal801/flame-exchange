// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "WIRE_INITIATION" and wire_type =~ "international" and beneficiary_country_first_time =~ true and transaction_amount >= 5000) and (not(beneficiary_bank_relationship =~ "established"))
