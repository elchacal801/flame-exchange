// FLAME logsource mapping -- table names and field names require environment-specific tuning.

((event_type contains "sim_swap" or event_type contains "carrier_change" or event_type contains "number_port") and status =~ "completed") and (event_type =~ "login_attempt" and mfa_method =~ "sms" and mfa_result =~ "success")
