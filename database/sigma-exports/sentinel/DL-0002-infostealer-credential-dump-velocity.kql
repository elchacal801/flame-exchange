// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "credential_access" and action =~ "credential_read") and (not((process_name endswith "\\lsass.exe" or process_name endswith "\\services.exe")))
