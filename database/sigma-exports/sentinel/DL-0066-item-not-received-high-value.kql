// FLAME logsource mapping -- table names and field names require environment-specific tuning.

(event_type =~ "dispute_filed" and dispute_reason =~ "item_not_received") and order_total_usd >= 500 and (carrier_delivery_status =~ "delivered" or account_inr_claims_90d >= 2)
