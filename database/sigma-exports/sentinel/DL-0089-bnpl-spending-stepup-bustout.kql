// FLAME logsource mapping -- table names and field names require environment-specific tuning.

account_type =~ "bnpl" and first_order_value <= 50 and max_order_pct_of_limit >= 85 and days_first_to_max_order <= 14
